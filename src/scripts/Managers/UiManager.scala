package scripts.Managers

object UiManager extends Manager[] {

  /**
   * Perform any necessary setup when the manager is first created.
   * Called once at application start.
   */
  override def init(): Unit = {}
}
